<Window x:Class="ScannerBridge.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تنظیمات اسکنر" Height="400" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0 0 0 10">
            <TextBlock Text="انتخاب اسکنر پیش‌فرض" />
            <ComboBox Name="ScannerComboBox" Width="300" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0 0 0 10">
            <TextBlock Text="رزولوشن (DPI)" />
            <TextBox Name="DpiTextBox" Width="100" Text="300"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0 0 0 10">
            <TextBlock Text="حالت رنگ" />
            <ComboBox Name="ColorModeComboBox" Width="150">
                <ComboBoxItem Content="Color" IsSelected="True"/>
                <ComboBoxItem Content="Grayscale"/>
                <ComboBoxItem Content="BlackWhite"/>
            </ComboBox>
        </StackPanel>

        <CheckBox Grid.Row="3" Name="AdfCheckBox" Content="استفاده از ADF" IsChecked="True"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0 10 0 10">
            <TextBlock Text="ترجیح استفاده از:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <RadioButton Name="TwainRadio" Content="TWAIN" GroupName="api" IsChecked="True"/>
            <RadioButton Name="WiaRadio" Content="WIA" GroupName="api" Margin="10 0 0 0"/>
        </StackPanel>

        <Button Grid.Row="5" Content="ذخیره تنظیمات" Width="150" HorizontalAlignment="Center" Click="SaveSettings_Click"/>
    </Grid>
</Window>
